var sound = 'ROAR!'

// Only change Line 4
function lion (noise) {
  return 'The lion goes, "' + sound + '"'
}

module.exports = lion('Meow...')

//changed parameter name so that it wasnt the same name as sound.
